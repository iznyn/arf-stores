{
  "name": "store-admin-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Daily Product Store Admin Dashboard - Turborepo Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.8.14"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=20.9.0"
  }
}
